export type ThemeMode = "light" | "terminal" | "system";

export type Theme = {
  background: string;
  surface: string;
  surface2: string;
  text: string;
  textSecondary: string;
  border: string;
  accent: string;
  today: string;
  glassBackground: string;
  glassBorder: string;
};
