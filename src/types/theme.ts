export type ThemeMode = "light" | "dark" | "system" | "amoled";

export type Theme = {
  background: string;
  surface: string;
  surface2: string;
  text: string;
  textSecondary: string;
  border: string;
};
