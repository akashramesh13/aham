export type ThemeMode = "light" | "dark" | "system";

export type Theme = {
  background: string;
  surface: string;
  surface2: string;
  text: string;
  textSecondary: string;
  border: string;
  accent: string;
  heroAccent: string;
  today: string;
};
